# Changelog

Все заметные изменения POLER-Quantum-RS документируются здесь.
Формат следует [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версионирование — [SemVer](https://semver.org/lang/ru/).

## [0.3.2] — RQ8: `pqc train` — плотный LENS-граф и накопительная память

### Добавлено

**CLI `pqc train` (накопительное глубокое обучение):**

- Один движок `StreamEngine` на весь корпус: блоки `--block` Б (по
  умолчанию 8 КиБ) льются подряд, union support дуг накапливается
  между блоками — против «затирания памяти последним чанком» в
  `pqc stream` (движок там создаётся заново на каждый вызов, а `--out`
  пишет контейнер свидетельства последнего чанка);
- Снапшот сериализует именно **накопленную память** `engine.model()`:
  чекпоинт `--out` (.pqw) + сырой Packed4-отпечаток `--fingerprint`
  (формат `cpu_phase_fingerprint.bin`);
- Живой мониторинг: `--snapshot-every N` блоков обновляет файлы на
  диске; состояние отпечатка читается проф-утилитами (`pqc inspect`,
  hexdump, radare2) прямо во время обучения;
- Телеметрия `--log` (по файлам: tokens/blocks/loss/nnz/support),
  `--json`-отчёт, `--corpus DIR` (рекурсивный обход, 29 текстовых
  расширений, файлы ≤ 8 МиБ) или `--stdin`;
- Плотный режим по умолчанию: ε=0.05, блок 8 КиБ, d_pol=4096,
  shots=20000, steps=10.

**Эксперимент «плотный снимок» (корпус 531 файл / 11.5 МиБ):**

- `cpu_phase_fingerprint.bin`: nnz 10 → **2240 дуг** (54.69% d_pol,
  1180 × −1 / 1060 × +1), энтропия 0.098 → **6.10 бит/байт**,
  вердикт crypto-recon: structured → **compressed**;
- Живой монитор `pqc inspect --json` каждые 2.5 с: 216 замеров,
  кривая роста nnz и насыщение (плато) — `fingerprint_monitor.jsonl`;
- `pqc unfurl dense_learned_state.pqw` — AOT-развертка плотного
  аттрактора в живой Rust-синтаксис (125 Б морфем: `fn`, `let`,
  `impl`, `i64`, `psi`, `fock`, `self`...);
- График: `download/lens_phase_map_comparison.png` (квантовый фон →
  кристаллическая решётка), `lens_training_dynamics.png`,
  `lens_graph_dense_sfdp.png` (2240 узлов).

### Тесты

- 3 новых интеграционных (`tests/train_cli.rs`): накопление union
  support двух файлов, валидность чекпоинта/отпечатка (magic
  `POLER_Q2`, version 2, nnz @ 0x60), JSON-отчёт, коды ошибок
  (usage 2 / пустой корпус 1). Всего в воркспейсе 317
  (pqc 210 + pqw 107).

## [0.3.1] — pqc inspect + RQ7-Alpha: AOT SyntaxUnfolder, CPU Phase Fingerprint, Deep/Internet Training

### Добавлено (RQ7-Alpha, от 6c4d4cd)

- **`pqc::syntax_unfolder`** — детерминированный AOT-кристаллизатор:
  безветвистая (branchless, zero-alloc) развертка фазового вектора p*
  в символьный поток через предвычисленную таблицу морфемных
  кристаллов 256 × 4 Б (L1 cache, генерируется в `build.rs`);
- **CLI `pqc unfurl <file>`** — AOT phase unfurling to syntax;
- **Чекпоинты обучения**: `master_learned_state.pqw` (24 095 файлов,
  20.7 мин CPU) и `internet_master_state.pqw` (краулер RFC/kernel/Wikipedia),
  телеметрия `*_training_telemetry.log` (~45К строк);
- **`cpu_phase_fingerprint.bin`** — сырой Packed4-снимок фазового
  состояния (1024 Б, d_pol=4096); `graph_visualizer.py` — web-визуализатор;
- `docs/training_and_evolution.md` — хроника 5-уровневой лестницы обучения.

### Добавлено

**Модуль `pqc::inspect` (полная «логика данных» произвольного файла):**

- Детект формата: контейнеры `POLER_QW` (v1) / `POLER_Q2` (v2),
  **raw-Packed4 кандидат** (файл без заголовка, все 2-битные пары ≠ 0b11,
  непечатаемый фон → d_pol = 4 × размер), `opaque` для остального;
- **Крипто-разведка**: Шенноновская энтропия (глобально + по блокам 64 Б),
  χ² против равномерного (df = 255), словарь сигнатур шифро-/архиво-
  контейнеров (OpenSSL `Salted__`, GPG, age, PEM, ZIP, GZIP, 7z, xz,
  zstd, PNG/JPEG/PDF/ELF/SQLite); вердикты `structured` / `compressed` /
  `encrypted-like` — «есть ли шифр» видно без ключа;
- **Побайтовая карта заголовка** `.pqw` (17 полей: magic, версии,
  гиперпараметры, digest, смещения секций, nnz, flags, checksum FNV-1a64);
- **Декод дуг**: v1 (трит + σ → p̂ → θ̂) и v2/raw (Packed4 → ±1);
  Born-энтропия `Σ h₂((1−p̂)/2)` и теоретический QCM `1 − H/d`;
- **Граф LENS**: рёбра = соседние хранимые дуги `u_k → u_{k+1}`
  (семантика `Entanglement::FromTopology`); вывод CSR-текстом,
  Graphviz DOT (`--dot`), JSON (`--json`) и ASCII-матрицей при `d_pol ≤ 64`;
- **Целостность**: header checksum (валидирована ридером), payload digest
  SHA-256 Trunc-24 (OK/FAIL), McWeeny-невязка записанная и пересчитанная;
- hex-дамп с ASCII-колонкой (`--hex N|all`), извлечение ASCII-строк
  (`--strings`), `--raw-dim N` — принудительная трактовка raw-Packed4.

**CLI `pqc inspect`:**

```console
$ pqc inspect cpu_phase_fingerprint.bin --graph
FORMAT: raw-packed4 (candidate), d_pol=4096, nnz=10 (8 × −1, 2 × +1)
CRYPTO RECON: entropy 0.098/8.0, chi2 256033 (NOT uniform) — structured,
  шифрования НЕТ; ARCS: 469, 900, 1404, 1568(+), 2436, 2732, 2796,
  3353, 3603(+), 3622; GRAPH: nodes 10, edges 9, density 0.244%
$ pqc inspect state.pqw --all      # карта + hex + все дуги + граф + строки
$ pqc inspect blob.bin --json      # машинно-читаемый отчёт
```

### Тесты

- 25 новых (15 unit + 9 integration + 1 doc): детект четырёх видов файлов,
  точные позиции raw-Packed4 дуг, вердикты structured/encrypted-like
  (детерминированный xorshift-поток), порча v2-payload с сохранением nnz
  → digest FAIL, DOT-файл с рёбрами, JSON собственным zero-dep парсером.
  Всего в воркспейсе 314 (pqc 207 + pqw 107), против 287 в v0.3.0.

## [0.3.0] — RQ6: Zero-Storage Streaming Learning Engine (Active Inference)

### Добавлено

**Модуль 1 — `pqc::stream_engine` (непрерывный потоковый инференс):**

- `StreamEngine` — сквозной монолитный цикл
  «HTML/текст → TF-IDF ε-плотность → LENS-CSR → Packed4 (реюз буфера) →
  коммутатор Фокиана → Born-фазовый шаг → QCM» целиком в RAM;
- **TF-IDF ε-плотность потока**: токены хешируются FNV-1a64 (координата +
  знаковая полярность, как в `pqw::stream`), внутри чанка накапливается
  знаковый `tf_i`, между чанками — частота документов `df_i`;
  вес `score_i = tf_i · ln((1+N)/(1+df_i))` (N включает текущий документ),
  L∞-нормализация в `[−1,1]^d`, LENS-фильтр `|p| ≥ ε`; сквозной шум
  (координата во всех чанках) давится `idf → 0`, отличительные токены
  усиливаются;
- **переиспользуемый Packed4-буфер**: контейнер v0.2 (4 трита/байт,
  magic `POLER_Q2`, d=65536 → 16 КиБ) собирается в `Vec<u8>` с
  сохранением ёмкости — после прогрева ни одной системной аллокации на
  контейнер (тест стабильности capacity);
- **семантический коммутатор (барьер NO_HITS)**:
  `fock_residual(f, ρ)` — Frobenius-норма симметризованного коммутатора
  ранговых Born-операторов `F = f·fᵀ`, `DM = ρ·ρᵀ` за O(d) без матриц:
  `‖[F,DM]‖_F = |fᵀρ|·√(2(‖f‖²‖ρ‖² − (fᵀρ)²))`; ровно нуль при
  пустом свидетельстве (детерминированный отказ без галлюцинаций), нуль
  на стационаре (`f ∥ ρ`) и при ортогональной новой теме (`f ⊥ ρ`);
- **политики фона Π_Λ**: `Forget::Hold` (по умолчанию — LENS-граф только
  накапливает факты, оператор резонанса J) и `Forget::Decay` (вне чанка
  цель 0 — диссипация старых тем, оператор D = LLᵀ); ρ-гиперпараметр
  контейнера кодирует политику (1.0/0.0);
- **цель обучения — непрерывная TF-IDF плотность** на LENS-дугах:
  контейнер хранит тритовую проекцию (сериализованное свидетельство),
  фазовый континуум обучается до самой McWeeny-пурификации;
- `strip_html` — zero-dependency срезание разметки (теги, `<script>`,
  `<style>`, комментарии, кавычки в атрибутах) с декодированием
  HTML-сущностей (именованые + числовые десятичные/шестнадцатеричные);
- union-support памяти модели: новые дуги входят с фазы честной монеты
  `p = 0` (мнение рождается измерением), `StreamChunkReport` — токены,
  nnz, размер контейнера, ёмкость буфера, коммутатор, шаг, потеря, QCM,
  задержка;
- **замеры (release, x86_64)**: сквозной ingest d=512 — **~0.32 мс**
  (бюджет DoD 5 мс, запас 15×), d=4096 — ~0.69 мс, 8 шагов обучения —
  ~0.73 мс, QCM + коммутатор изолированно — **~50 мкс** (бюджет 150 мкс),
  throughput **~1.36M токенов/с** (`examples/active_inference_bench.rs`).

**Модуль 2 — `pqc::learn::ActiveInference` (адаптивная петля):**

- **расписание шага по сюрпризу**: `η(t) = η₀·exp(−β·Σ(t))`, где
  `Σ(t)` — нормированная Born-энтропия активных дуг (сюрприз модели);
  в хаосе (Σ → 1) шаг сжимается до `η₀·e^(−β)` — термодинамическое
  трение гасит шум среды; у стационара (Σ → 0, фазы ≈ триты) шаг
  возвращается к η₀ — уравнение субъективного времени `T = dI/dΣ`;
- регламент по умолчанию RQ6: **γ = 0.5** (полюсная защита RQ5 — эмпирически
  подтверждено: при K=5 сильный момент γ ≥ 0.8 осциллирует у полюсов и не
  даёт пурификации дожать треки до residual < 1e−6), **McWeeny K = 5**;
- **строгая проекция Π_Λ** (улучшение RQ6 против BornOptimizer): у
  замороженных дуг обнуляется не только градиент, но и момент — фона не
  дрейфует от инерции истории (у Hold остаётся только затухающий хвост
  перелёта ≤ η·v/(1−γ));
- `ActiveStepReport` — сюрприз, адаптивный η, суррогатная потеря, норма
  градиента, шум измерения, флаг пурификации; `eta_at(Σ)`,
  `ActiveInference::surprise(ansatz)` — детерминированное расписание
  (по теоретическим маргиналам, без шума измерения);
- DoD: адаптивная сходимость ≤ 10 шагов (product d=512, ↓ > 20×),
  отслеживание regime-jump при γ = 0.5 (↓ > 100×), пурификация K=5 до
  чистых тритов (residual < 1e−6), η ≤ η₀ всегда и растёт по мере
  падения сюрприза, побитовая воспроизводимость.

**Модуль 3 — CLI и бенчмарки:**

- `pqc stream (--url | --file | --text | --stdin)` — сквозной парсинг и
  квантовая оценка страницы за миллисекунды: `--dim`, `--epsilon`,
  `--shots` (default 10000), `--steps`, `--seed`, `--eta0`, `--beta`,
  `--gamma`, `--decay`, `--json` (zero-dep JSON-отчёт), `--out` (дамп
  Packed4-контейнера — единственная опциональная точка касания диска);
- **zero-dep HTTP-клиент** на `std::net::TcpStream`: HTTP/1.1 GET,
  редиректы (≤ 3), `Transfer-Encoding: chunked`, лимит тела 16 МиБ,
  таймауты 15 с; https сознательно отвергается с подсказкой сохранить
  страницу и передать `--file` (TLS несовместим с нулём зависимостей);
- `examples/active_inference_bench.rs` — throughput (токены/с), латентность
  сквозного цикла, изолированные QCM + коммутатор, честный FLOPs-отчёт
  против плотного self-attention `2·n²·d` (для чанка 500 токенов,
  d=512: **~1218× дешевле**; справочно из манифеста: Qwen2.5-1.5B
  792.7M dense против 36.7M POLER = 21.6×).

### Исправлено

- warning `unused import: std::path::PathBuf` в `tests/pqw_bridge.rs`.

### Документация

- `docs/quantum-core.md` §14 — RQ6: схема конвейера, математика TF-IDF,
  расписания η(t) и коммутатора, политики фона, замеры;
- README: секция RQ6, обновлённые CLI/тесты/roadmap;
- кросс-линк статуса в `POLER-Quantum/docs/rust-core-roadmap.md`.

## [0.2.0] — RQ5 + формат v0.2: петля обучения Born и Packed4-триты

### Добавлено

**RQ5 — петля фазового обучения Born (без внешних фреймворков):**

- `pqc::learn::BornOptimizer` — замкнутый цикл
  «Born-измерение → суррогатный градиент → фазовый шаг»:
  - измерение `p_emp,i = 1 − 2·N₁,i/N_shots` (несмещённая оценка
    параметра `E[p_emp] = p`);
  - параметрический сдвиг по фазе `∇_θ,i F = ∇_p,i F · (−√(max(0,1−p²)))`;
  - тяжёлый шар `v ← γ·v + Π_Λ[∇_θ F]`, шаг `θ ← arccos(p) − η·v`,
    перекодировка `p ← cos(θ)`;
  - проектор причинности Π_Λ: структурный носитель дуг (product-движок)
    ∩ порог LENS-значимости `|p| ≥ ε`;
  - McWeeny-пурификация `p ← 2(3λ²−2λ³)−1` каждые `K` шагов
    (заострение к чистым тритам, восстановление P² = P);
- `pqc::learn::quadratic_target` — каноническая квадратичная цель
  `∇F = p_emp − target` (суррогат Борна из симуляции владельца);
- `BornOptimizer::{parameters, set_parameters, measure}` — плотный доступ
  к фазам анзаца (statevector: перекодировка Ry-слоя; product: дуги);
- `Ansatz::engine()`, `PhaseAnsatz::set_arc_phases`;
- **DoD**: сходимость за ≤ 10 шагов на обоих движках (product d=512:
  потеря ↓ в 32×; statevector d=6: ↓ в 11×), монотонная сходимость,
  отслеживание regime-jump (инверсия цели за 10 шагов, ↓ в 415×),
  пурификация до чистых тритов (residual < 1e−6), побитовая
  воспроизводимость при фиксированном сиде;
- задокументировано явление **залипания на полюсах**: при `|p| = 1`
  производная `dp/dθ = −sin θ` вырождается, сильный момент (γ ≥ 0.8)
  при резкой инверсии цели заносит фазу на полюс — для отслеживания
  regime-jump'ов рекомендуется γ ≈ 0.5;
- `examples/learning_bench.rs` — стоимость одного градиентного шага:
  d=512 — ~2 мс, d=4096 — ~16 мс (1024 выстрела на измерение).

**Формат v0.2 — Packed4: сверхплотная упаковка тритов (4 дуги на байт):**

- новый magic `POLER_Q2` (`0x504F4C45525F5132`), `format_version = 2`;
  заголовок и контроль целостности (FNV-1a64 + SHA-256→24) — прежние;
- битовое отображение: `0b00 → Zero`, `0b01 → +1 (λ=1)`,
  `0b10 → −1 (λ=0)`, `0b11 → Reserved`; пары в LE-порядке — трит `j`
  байта `i div 4` занимает биты `2j..2j+1`; фон кодируется нулевыми
  байтами;
- фазовые блоки: `ceil(d_pol/4)` байтов вместо `nnz` — **ровно 4x**
  сжатие фазовых блоков относительно v1 (d=65536 → 16 КиБ); топологии
  в v2 нет (плотный массив), флаги обязаны быть нулевыми;
- `TritEncoding` (`Curved`/`Packed4`), `pack_trit2`/`unpack_trit2`,
  `pack_quad`/`unpack_quad`, `nearest_trit` (`|p| ≥ 0.5 → sign(p)`);
- `PqwWriter::{write_packed_trits, to_bytes_packed, write_packed_to,
  estimated_size_packed}`;
- `PqwReader::iter_packed_trits` — zero-copy итератор плотного массива
  (все d_pol позиций, включая Zero);
- канонический sparse-вид v2: `arcs()/decoded()/indices()` отдают только
  ненулевые триты — семантика идентична v1 (Zero ≡ фон ≡ честная монета),
  проверено тестом паритета v1↔v2;
- валидация v2: nnz = точный пересчёт ненулевых тритов, паддинг-пары
  хвостового байта обязаны быть Zero, код `0b11` — `ReservedTrit`;
- `PqwError::NotPacked` для v1-контейнеров в плотных операциях;
- CLI `pqw gen --packed` + честный вывод magic/кодировки в `info`;
- полная обратная совместимость v1 (все прежние golden-тесты зелёные).

### Изменено

- `PqwError::UnsupportedVersion`/`BadMagic` — сообщения охватывают оба
  поколения формата; пара magic ↔ версия проверяется перекрёстно.

## [0.1.2] — RQ3 + RQ4: Zero-Storage Streaming и паритет с Qiskit

### Добавлено

**RQ3 — zero-storage стриминг (полный конвейер в RAM):**

- `pqw::PqwWriter::write_to_vec` — сборка контейнера в чужой буфер без
  промежуточного файла (`to_bytes` теперь делегирует ему);
- `pqw::stream::TextPhaseEncoder` — эталонный кодировщик текстовых чанков:
  Unicode-токенизация → FNV-1a64-хеширование → накопление знаков →
  L∞-нормализация → ε-плотность LENS → триты; детерминизм без зависимостей;
- `pqc::stream::ZeroStoragePipeline` — сквозной конвейер
  «текст → ε-плотность → разреженный граф → ин-мемори .pqw → Born →
  метрика когерентности» без единого обращения к диску;
  **d = 512: ~105 мкс** (бюджет RQ3 — 2 мс, запас ×19);
- `pqc::coherence` — метрика квантовой когерентности QCM =
  `1 − Σ h₂(P(b_q=1))/d_pol` (теория и наблюдение, зазор — калибровка
  сэмплирования), двоичная энтропия `h₂`;
- `examples/stream_bench.rs` — замеры латентности конвейера.

**Шаг 3 — LENS-энтанглмент:**

- `pqc::entangle::{Entanglement, Entangler}` — режимы `None`,
  `Chain` (резонансный слой как в qiskit-анзаце), `FromTopology`
  (соседние LENS-дуги контейнера → CX/CZ-рёбра без материализации
  плотной матрицы N×N);
- product-движок: **точное** GF(2)-сэмплирование CX-слоёв (префиксный
  XOR слов — 6 сдвиговых шагов на u64), CZ невидим для измерений;
  маргиналы и вероятности паттернов — марковская прогонка; совпадение
  со statevector-гейтами до 1e-12 (тесты);
- `Ansatz::from_reader_entangled`, `PhaseAnsatz::with_entanglement`.

**RQ4 — χ²-паритет Rust (pqc) ↔ Python/Qiskit (poler_quantum.quantum):**

- `pqc::parity::ParityAnsatz` — точное зеркало `PolerAnsatz`:
  восприятие `R_y(arccos p)` → резонанс (CX/CZ-цепь) → стабилизация
  `R_z` (mode B — константа, mode C — адаптивная фаза
  `gamma·exp(−|sin(kappa·π)|)`);
- `pqc::parity::splitmix_phases` — языко-агностичный контракт фаз:
  `p_k = (x_k >> 11)/2^52 − 1`, бит-в-бит одинаков в Rust и Python;
- `pqc::parity::config_rng_seed` — независимые потоки выстрелов на
  конфигурацию (χ²-статистики режимов A/B/C не коррелируют);
- `pqc::json` — минимальный JSON без зависимостей (RFC 8259-подмножество,
  суррогатные пары, лимит глубины, бит-точный round-trip f64 через
  кратчайшую форму `{:e}`);
- CLI `pqc-parity` — экспорт конфигураций протокола `pqc-parity/1`
  (амплитуды re/im, Born-счётчики, фазы);
- `tests/qiskit_parity_runner.py` — Qiskit-раннер: точные амплитуды
  `Statevector`, χ² Пирсона с пулингом бинов (правило Кохрена, E < 5),
  семейство из трёх режимных агрегатов с поправкой Бонферрони
  (α′ = 0.05/3), явная проверка little-endian через argmax;
- `tests/cross_qiskit_parity.rs` — матрица **56 конфигураций**
  (сиды {42, 1337, 2026} × n ∈ {2, 4, 8} × режимы A/B/C × {cx, cz}
  по 100 000 выстрелов + 2 зонда конвенции битов).

### Результаты кросс-теста

- максимальное расхождение амплитуд **1.1e-16** (бюджет 1e-12);
- агрегаты режимов: A p = 0.415, B p = 0.030, C p = 0.492 — все выше
  Бонферрони-порога 0.0167; минимальный конфиг-p = 0.030 > пола 1e-4;
- контроль `numpy.multinomial` подтверждает: для идеального сэмплера
  ~5% конфигураций закономерно ниже 0.05 — семейный критерий
  обязателен (задокументировано в docs/quantum-core.md §10).

### Изменено

- `SampleReport` получил поле `d_pol` (точная размерность для
  product-движка — нужна метрике когерентности);
- `PqwError`/`PqcError`: добавлен вариант `LengthMismatch`;
- `rng::splitmix64` стал `pub(crate)` (используется контрактом фаз).

## [0.1.1] — RQ1: POLER Quantum Core

### Добавлено

- **Крейт `pqc`** — вычислительное ядро на чистом Rust (zero-dependency):
  - `Statevector`: 2^n комплексных амплитуд, гейты `R_y/R_x/R_z`, `H`,
    `X/Y/Z`, `CNOT`, `CZ`, произвольная однокубитная `U2`;
  - анзац POLER `from_phases`: `|ψ⟩ = ⊗ R_y(arccos p)|0⟩`;
    тождество `P(b=1) = (1−p)/2`, `λ = (1+p)/2 = P(b=0)`;
  - `BornSampler`: правило Борна через инверсию CDF (бинарный поиск),
    счётчики, топ-K, теоретические вероятности исходов;
  - `PhaseAnsatz`: LENS-разреженный продуктовый движок для любых
    `d_pol`; фон — честные монеты через popcount-трюк
    (`Binomial(64, ½)` за одно случайное слово), выстрел
    `O(nnz + d_pol/64)`;
  - `Ansatz::from_reader`: мост из контейнера `.pqw` (zero-copy),
    опциональная McWeeny-очистка перед кодированием и жёсткая
    верификация digest;
  - ГПСЧ xoshiro256++ с сеянием SplitMix64 и jump-потоками;
  - параллельность: детерминированный fork-join на `std::thread::scope`
    без rayon; авто-векторизуемые ядра (блочный обход пар амплитуд);
  - CLI `pqc run` / `pqc demo` — полностью автономный бинарник:
    mmap → контейнер → анзац → Born-отчёт (топ-K, маргиналы,
    статистика веса Хэмминга);
  - `examples/bench.rs` — замеры ядер.

### Исправлено

- CNOT-ядро: условие срабатывания инвертировано (свап выполнялся при
  control = 0) — поймано белловским тестом `cnot_builds_bell_state`.

### Документация

- `docs/quantum-core.md` — спецификация ядра: математика анзаца,
  семантика фона/спайков, SIMD-стратегия, замеры;
- README: секция «Вычислительное ядро pqc», обновлённые тесты и roadmap.

### Тесты

- 89 новых (86 unit/integration + 3 doc): паритет с аналитикой на 1e-12
  (включая состояние Белла), статистика Born в 5σ, мост pqw → pqc,
  mmap end-to-end, побитовая воспроизводимость. Всего в воркспейсе 157.

## [0.1.0] — pqw: формат .poler/.pqw

- Крейт `pqw`: zero-dependency контейнер фазовых весов — триты
  `{−1,0,+1}` + 6-битная кривизна σ, sparse LENS-топология (u16/u32),
  mmap zero-copy, SHA-256 Trunc-24 payload digest, FNV-1a64 checksum
  заголовка, McWeeny-инвариант `max|λ²−λ|` в заголовке.
- CLI `pqw gen / info / verify / dump`.
- 66 тестов + 2 doc-теста: NIST-векторы хешей, golden-layout,
  матрица повреждений ×15, roundtrip d=512…65537.
- Спецификация формата: `docs/pqw-format.md`.
